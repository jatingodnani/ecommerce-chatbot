# Docker Environment Configuration
# Copy this to .env for production deployment

# MongoDB Configuration
MONGODB_URL=mongodb+srv://godnanijatin:Vj7ZIuzkl5wX0aiY@cluster0.l0mhrl9.mongodb.net/ecommerce_chatbot?retryWrites=true&w=majority
DATABASE_NAME=ecommerce_chatbot

# Application Configuration
ENVIRONMENT=production
DEBUG=false

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# CORS Configuration (adjust for your domain)
ALLOWED_ORIGINS=["http://localhost:3000", "http://localhost", "https://yourdomain.com"]

# Logging
LOG_LEVEL=INFO

# Security (generate secure values for production)
SECRET_KEY=your-secret-key-here
JWT_SECRET=your-jwt-secret-here

# Optional: Local MongoDB (if using local instance)
# MONGODB_URL=mongodb://admin:password123@mongodb:27017/ecommerce_chatbot?authSource=admin
